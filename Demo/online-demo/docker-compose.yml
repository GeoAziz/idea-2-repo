version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://user:password@localhost:5432/ai-expense-tracker-for-freelancers
  database:
    image: mongo:6
    ports:
      - "27017:27017"